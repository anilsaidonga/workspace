meta {
  name: Add Part
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/api/parts
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessTokenAdmin}}
}

body:json {
  {
    "name": "Oil Filter",
    "quantity": 50
  }
}
