meta {
  name: Hold Vehicle
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/api/edge-cases/hold-vehicle
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessTokenAdmin}}
}

body:json {
  {
    "vehicleNumber": "TOW999",
    "type": "CAR",
    "fuelType": "DIESEL"
  }
}
